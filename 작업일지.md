# 작업일지 — 에코스섬의 비밀 퍼블리싱

## 개요
- 프로젝트: EBS 이솜소프트웨어 x VRWARE "에코스섬의 비밀" 웹 퍼블리싱
- 정적 HTML 페이지 → React(Vite) 프로젝트로 전환 완료
- 리포지토리: https://github.com/globepoint-github/ECOS (master 브랜치)
- 로컬 실행: `npm install` → `npm run dev` (http://localhost:5173)

## 완료된 기능

### 1. 메인 화면 (타이틀)
- 배경 + 타이틀 로고 + 좌우 행성 이미지
- 어떤 화면 비율이든 안 잘리고 꽉 차게 표시
- 행성이 둥둥 떠다니는 애니메이션
- 하단 매뉴얼 다운로드 / START 버튼

### 2. 스테이지 선택 화면
- 7단계 원형 버튼 + 단계 사이 연결선 + 이동하는 로켓
- 클릭한 단계는 활성(노랑), 이미 깬 단계는 완료(청록) 상태로 표시
- 3단계, 7단계 클리어 시 각각 인증서 받기 버튼이 활성화됨
- 태블릿 화면 크기에서도 레이아웃 안 깨지게 대응

### 3. 잠긴 스테이지 안내 팝업
- 아직 못 깬 단계를 클릭하면 "이전 미션을 완료해주세요" 안내 팝업 표시

### 4. 유니티 게임 연동
- 스테이지 클릭 시 새 탭으로 유니티 게임 실행
  - 일반판 → `/game`, 코딩파티판 → `/game2`
- ⚠️ 아직 실제 게임 파일이 안 들어가 있는 빈 폴더 상태 (게임 파일 + API 연동은 남은 작업)

### 5. 인증서(수료증) 기능
- "인증서 받기" 버튼 클릭 → 별도 브라우저 창으로 인증서 페이지 오픈 (실제 EBS 사이트처럼 닫기/최대화 되는 진짜 새 창)
- 1단계 / 2단계 인증서 각각 지원
- 오늘 날짜 자동 표시, 저장하기(PNG 다운로드) / 인쇄하기 버튼 작동
- 창 크기를 키워도 화질이 뭉개지지 않게 처리
- 인증서 받기 버튼이 아직 활성화 안 됐을 때 누르면 안내 팝업 표시
- ⚠️ 이름 입력칸은 일단 제거한 상태 (어떤 정보를 받을지 다시 정해지면 추가 예정)

## 다음 할 일 (TODO)
- 코딩파티판 전용 이미지/텍스트 받으면 해당 화면 채우기
- 인증서에 넣을 입력 항목(이름 등) 다시 정하고 추가
- 유니티 게임 파일 넣고 API 연동
- 로그인 / 진행도 서버 저장 여부 결정
- 디자인팀 피드백 반영
