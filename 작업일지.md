# 작업일지 — 에코스섬의 비밀 퍼블리싱

## 1. 개요
- 프로젝트: EBS 이솜 소프트웨어 x VRWARE "에코스섬의 비밀" 웹 퍼블리싱
- 초기: 정적 `index.html` 한 파일로 시작 → 최종적으로 React(Vite) 프로젝트로 전환
- 리포지토리: https://github.com/globepoint-github/ECOS
- 현재 기본 브랜치: `master`

## 2. 메인 화면 (타이틀 페이지)
- 배경(`메인화면 복사 1.png`, 1920×1080) 위에 타이틀(`Group (4).png`), 좌우 행성(`메인화면_1 1.png`, `메인화면_2 2.png`) 레이어로 배치
- 화면 어떤 비율이든 배경이 잘리지 않고 화면을 꽉 채우도록 `object-fit: cover` 방식 채택 (여백 vs 크롭 vs 왜곡 트레이드오프 논의 후 크롭 방식으로 확정)
- 하단 배너: 로고 + "매뉴얼 다운로드"(퍼플) / "START"(블루) 버튼, Figma 스펙 기준 크기·패딩·폰트 반영
- 폰트: `ONE Mobile POP` (jsDelivr CDN, projectnoonnu 리포지토리)
- START 버튼 너비를 매뉴얼 버튼과 정확히 일치시키기 위해 JS로 `offsetWidth`를 실시간 동기화
- 좌우 행성에 `@keyframes floatPlanet`으로 위아래로 둥둥 뜨는 애니메이션 추가

## 3. 스테이지 선택 화면 (START 클릭 후)
- 배경(`image 2482.png`) + 잠금 원 7개(1~7단계) + 원 사이 점선 연결선 6개 + 로켓 + 매뉴얼/인증서 버튼으로 구성
- 각 원/연결선은 Figma 스펙(픽셀 크기)을 1920×1080 기준 `%`로 환산해 배치
- **스테이지 진행 로직**: 클릭한 원 → 노란색(활성) 이미지로 전환 + 로켓이 해당 스테이지 위치·각도로 이동, 이전 스테이지는 "잠금해제 기본"(청록색) 이미지로 복귀, 다음 스테이지는 미리 잠금해제 기본 이미지로 바뀌어 클릭 유도
  - 원본 잠금(회색), 잠금해제 기본(청록/완료), 잠금해제(노랑/활성) 3단계 이미지 세트를 스테이지별로 매핑
  - 3단계·7단계 클리어 시 각각 좌/우 "인증서 받기" 버튼이 활성화(컬러) 이미지로 전환, 한 번 활성화되면 유지
  - 7단계는 최종 스테이지라 연결선이 없고, 로켓이 로봇 동승 + "CLEAR" 말풍선이 그려진 전용 이미지(`Group 1000014461.png`)로 교체됨
- 원/버튼에 hover 시 살짝 확대 + 밝기 증가 효과 추가
- 로켓(START 대기 로켓, 스테이지 이동 로켓) 둥둥 뜨는 애니메이션 추가 (`margin-top` 오간닥거림으로 구현 — 기존 회전/반전 `transform`과 충돌 안 나게 분리)
- 태블릿 폭(700~1400px) 구간에서 화면이 잘리는 문제 → `.selectScene` 전체를 하나의 단위로 `scale(0.85)` 축소해 PC와 동일한 레이아웃을 유지한 채 여백만 생기도록 처리 (아이패드 프로 가로모드 1366px까지 포함하도록 범위 확장)

## 4. 이미지 비율 보정
- 잠금 원(회색/노랑) 이미지가 Figma 원본 파일 자체 가로세로 비율과 달라 살짝 타원으로 보이던 문제를 발견해 각각 실제 파일 비율(279:313, 311:344)에 맞게 `height` 재계산
- 노란 연결선(6-7) 이미지가 회색 버전과 원본 비율이 달라 박스에 맞추면서 휘어 보이던 문제 → `.connector`에 `object-fit: contain` 적용해 비율 유지하며 박스 크기는 회색과 동일하게 고정

## 5. Git / 배포
- GitHub 저장소(`globepoint-github/ECOS`) 생성 후 `HTML` 브랜치로 최초 정적 버전 커밋·푸시
- 이후 `normal`, `cp` 브랜치를 만들었다가 삭제하고, `master` 브랜치를 새로 생성
- 로컬 네트워크 공유용으로 Node 기반 임시 정적 서버(포트 8000)를 띄워 팀 내부 IP로 공유

## 6. React(Vite) 구조 전환 (master 브랜치)
- 정적 `index.html` 한 파일 → Vite + React 프로젝트로 전환 (기능·CSS 100% 동일하게 포팅)
- 구조:
  ```
  src/
    App.jsx           # 1단계 분기: /coding-party/* → CodingPartyApp, /* → NormalApp
    NormalApp.jsx      # 2단계 분기: "/" 메인, "/stage" 스테이지 선택
    CodingPartyApp.jsx # 2단계 분기 (구조 동일, 콘텐츠는 스텁)
    pages/
      NormalMainPage.jsx / NormalStagePage.jsx   # 실제 구현
      MainPage.jsx / StagePage.jsx               # 코딩파티판 스텁 (Normal 컴포넌트 그대로 렌더링)
    styles/main.css     # 기존 <style> 전체 포팅
  public/images/...     # 이미지 전부 이동, 절대경로(/images/...)로 통일
  ```
- 코딩파티(이벤트)판과 일반판을 URL 경로만으로 분기하는 구조를 미리 잡아둠 — 코딩파티 전용 에셋이 들어오면 `MainPage.jsx`/`StagePage.jsx` 내부만 채우면 되는 상태
- `npm run build` 정상 빌드 확인, `vite preview`로 메인/스테이지 화면 렌더링 결과가 기존과 동일함을 스크린샷으로 확인
- 로컬 실행: `npm install` → `npm run dev` (http://localhost:5173/)

## 7. 잠긴 스테이지 안내 팝업
- 클리어 전 스테이지(잠긴 원)를 클릭하면 `Group 1000014528.png`(이전 미션을 완료해주세요 안내 이미지) + X 닫기 버튼(SVG) + "닫기" 버튼으로 구성된 오버레이 팝업 표시
- 오버레이 클릭 또는 X/닫기 버튼 클릭 시 팝업만 닫히고 잠금은 그대로 유지(스테이지 진입 안 됨)
- X 아이콘은 SVG `<line>` 두 개로 직접 그림 — 끝점을 뷰박스 모서리에서 살짝 안쪽으로 오프셋(2.5, 21.5)해서 round cap이 잘려 보이던 문제 해결

## 8. 유니티 게임 실행 연동
- 잠금 해제된 스테이지 클릭 시 `window.open(`${gamePath}/index.html?step=${n}`, '_blank')`로 유니티 빌드 결과물을 새 탭에서 실행
- `gamePath`는 일반판 `/game`, 코딩파티판 `/game2`로 분리 (`NormalStagePage`에 props로 전달)
- 클릭 시 해당 스테이지가 활성화(로켓 이동, 다음 스테이지 잠금해제, 3/7단계면 인증서 버튼 활성화)되면서 동시에 게임 탭이 열림
- `public/game/`, `public/game2/`는 현재 빈 폴더(README만 존재) — 실제 유니티 빌드 파일과 API 연동은 이후 작업

## 9. 인증서(수료증) 기능
- 처음엔 페이지 내 모달(오버레이)로 구현했다가, 실제 운영 사이트(ebssw.kr) 레퍼런스처럼 **완전히 별도의 브라우저 창**으로 열리도록 구조 변경
  - 라우트: `/certificate/:certId` (1 = 1단계 인증서, 2 = 2단계 인증서)
  - 스테이지 화면의 "인증서 받기" 버튼 클릭 시 `window.open('/certificate/1', '_blank', 'width=1050,height=700,noopener')`로 새 창 오픈 (닫기/최대화 등 브라우저 기본 창 컨트롤 그대로 사용)
- `Certificate.jsx` 컴포넌트가 이미지·이름 입력칸·날짜·저장/인쇄 버튼을 렌더링, `imageSrc`/`nameBgColor` props로 1단계(연두 `#d3ff82`)/2단계(라벤더 `#d5c9ff`) 인증서를 같은 로직으로 재사용
- 이름 입력: 실제 타이핑 값은 자간 0, placeholder("이름")만 Figma 스펙 자간(66.5%) 적용 — 자간을 실제 값에도 주면 마지막 글자 뒤 여백 때문에 중앙정렬이 틀어져 보이는 문제 해결
- 날짜: `new Date()` 기준 항상 오늘 날짜로 자동 표시, 폰트는 Figma 스펙대로 Oagothic Medium(500) 유지 (ExtraBold는 너무 두꺼워서 되돌림, 세미볼드(600)는 해당 무료 폰트에 없어서 불가 확인함)
- 저장하기: `<canvas>`에 배경+이름+날짜를 그려서 PNG로 다운로드
- 인쇄하기: 같은 방식으로 canvas 렌더링 후 새 창에 이미지로 띄우고 `onload`에서 `window.print()` 자동 호출
- **팝업 창 크기 대응** (이번 세션 마무리 작업): 인증서 원본 이미지는 1200×800 고정 해상도인데, 사용자가 창을 마우스로 최대화하면 그보다 훨씬 크게 늘어나면서 화질이 뭉개지는 문제 발견
  - 창이 원본보다 작을 때는 지금처럼 여백 없이 꽉 채우고(cover), 원본(1200×800)보다 커지면(최대화 등) 더 이상 확대하지 않고 원본 크기 그대로 위쪽에 붙어서 보이도록 수정 (`width: min(1200px, max(100vw, 150vh))`, `min-width`+`max-width`를 같이 쓰면 min이 항상 이겨서 안 먹히는 CSS 특성 때문에 계산식 하나로 통일)
  - 세로 중앙 정렬 대신 위쪽 정렬로 변경 — 실제 운영 사이트(ebssw.kr) 레퍼런스와 동일하게 카드가 화면 중앙이 아니라 상단에 붙고, 남는 공간은 아래쪽에 생기도록 처리
- **인쇄하기 미리보기 창 대응**: 인쇄 버튼으로 뜨는 별도 미리보기 창은 `certCard`와 무관한 `document.write`로 만든 순수 HTML이라 위 크기 대응 로직이 적용 안 돼 있던 문제 발견
  - 처음엔 certCard와 같은 `min(1200px, max(100vw, 150vh))` 계산식을 그대로 적용했더니, 인쇄 미리보기는 브라우저 창이 아니라 실제 인쇄 페이지(A4 등, 폭이 좁음) 기준으로 렌더링되면서 `150vh`가 페이지 폭보다 훨씬 커져 왼쪽이 잘려 보이는 문제 발생
  - 인쇄 쪽은 화면 꽉 채우기가 필요 없으므로 단순하게 `width:100%; max-width:1200px;`로 변경 — 페이지 폭에 맞게 줄어들되 원본 해상도 이상으로는 안 커지게 처리

## 10. 다음 할 일 (TODO)
- 코딩파티판 전용 이미지/텍스트 에셋 확보 후 `MainPage.jsx`, `StagePage.jsx`, `CertificatePage.jsx` 채우기
- 로그인/진행도(clearedStep) 연동 여부 결정 (현재는 클릭 시 즉시 어떤 스테이지든 진입 가능한 상태, 서버 연동 없음)
- `public/game`, `public/game2`에 실제 유니티 빌드 파일 배치 + API 연동
- 디자인팀 피드백 반영 (배포 링크 공유 예정)
